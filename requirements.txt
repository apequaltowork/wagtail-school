Django>=5.2,<5.3
wagtail>=7.4,<7.5
# psycopg2-binary>=2.8,<2.9  -- dropped at 2.15: no Python 3.10 wheels (source build needs MSVC); see UPGRADE_LOG B01
psycopg2-binary>=2.9,<2.10
# Unidecode: used directly by core/management/commands/seed_demo.py (slugs, demo emails).
# Came in transitively with Wagtail 2.7; pinned at 2.15 for the clean_name backfill (B04); kept at 7.4 for seed_demo (B20)
Unidecode>=1.1,<2
